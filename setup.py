from setuptools import setup, find_packages

setup(name='BMI-Calculator',
      version='1.0.0',
      author='Sachin Waghmare',
      packages=find_packages(),
      include_package_data=True,
      zip_safe=False)